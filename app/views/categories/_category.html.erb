<div class="<%= wrap_class %>">
  <%= link_to category.name, category_path(category), class: "govuk-body strong_link" %>
  <p class="govuk-!-font-size-16 govuk-!-margin-top-1"><%= category.description %></p>
</div>
