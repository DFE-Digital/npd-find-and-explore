<%= render 'shared/search_bar' %>

<div class="govuk-grid-row">
  <h3 class="govuk-grid-column-full govuk-heading-m"><%= t('.title') %></h3>
</div>

<div class="govuk-grid-row">
  <%= render partial: 'category', collection: @categories, locals: { wrap_class: 'govuk-grid-column-one-third govuk-grid-column-same-height' } %>
</div>
